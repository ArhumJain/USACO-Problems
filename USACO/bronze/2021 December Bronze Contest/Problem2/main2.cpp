#include <iostream>
using namespace std;
int main() {

    int N; cin >> N;
    int p[100000];
    int t[100000];
    int commands = 0;

    for (int a=0; a<N; a++) cin >> p[a];
    for (int a=0; a<N; a++) cin >> t[a];
    int currChange = 0;
    for (int a=0; a<N; a++) {
        if (p[a]-t[a] > 0) {

        }
    }
}